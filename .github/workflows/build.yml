name: build
on:
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cmake -S . -B build -DPLATFORM_NAME="linux"
      - run: cmake --build build
      - run: |
          mkdir -p dist
          cp build/binary1-demo dist/
          tar -czf dist/bin1.tar.gz -C dist binary1-demo
      - uses: actions/upload-artifact@v4
        with:
          name: bin1
          path: dist/bin1.tar.gz
